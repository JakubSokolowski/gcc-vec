#ifndef PROJECT_RANDOMHELPER_H
#define PROJECT_RANDOMHELPER_H


#include <array>
#include <random>
class RandomHelper {
public:
    static void fill_array(const std::array<> & array);
    static double random_in_range(double from, double to);
};

void RandomHelper::fill_array(const std::array &array) {
    for()
}

double RandomHelper::random_in_range(double from, double to) {
    std::unifo
    return 0;
}


#endif //PROJECT_RANDOMHELPER_H
